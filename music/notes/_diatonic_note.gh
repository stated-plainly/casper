import NoteLetter from @path(root/shared);
import NoteModifier from @path(root/shared);

export class template DiatonicNote : Note {
	constructor (letter : NoteLetter, modifier : NoteModifier) {
		super(letter, modifier);
	}
}
